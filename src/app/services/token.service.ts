import {privateKey} from "../privateKey";
//const jwt = require('jsonwebtoken');

class TokenService {
  constructor() {
  }
  createToken(obj: object) {
    return //jwt.sign({data: obj}, privateKey, {expiresIn: '100y'})
  }
  getCookie(cookieName: string) {

  };
}

